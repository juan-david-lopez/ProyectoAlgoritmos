# ============================================================================
# RAILWAY.TOML - Configuración para Railway
# ============================================================================

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.cli"

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:$PORT --workers 2 --timeout 300 api_server:app"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 10

[env]
PYTHONUNBUFFERED = "1"
